def username = 'Jenkins'
echo 'Hello Mr.${username}'
echo "I said, hello Mr.${username}"

node('jenkins-jnlp') {
    stage ('Build'){
            echo "Build"
    }
    stage ("test") {
            echo "test"
    }
    stage ("deploy"){
            echo "deploy"
    }
}
