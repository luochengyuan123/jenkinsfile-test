node('jenkins-jnlp') {
    stage ('Build'){
        steps {
            echo "Build"
        }
    }
    stage ("test") {
        steps{
            echo "test"
        }
    }
    stage ("deploy"){
        steps{
            echo "deploy"
        }
    }
}
